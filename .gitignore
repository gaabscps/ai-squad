node_modules/
dist/
*.log
aios.config.json

.superpowers/

# ai-squad per-repo hook install (managed by `ai-squad deploy`)
.claude/hooks/

# ai-squad session state (dogfood FEATs neste repo; regenerável, não é fonte)
.agent-session/

# cache do aiOS (resumos de task gerados por IA; regenerável, não é fonte)
.aios-cache/
