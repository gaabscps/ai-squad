{
  "name": "ai-squad",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "os": "npm run dev -w @ai-squad/os",
    "test": "npm test --workspaces --if-present"
  }
}
