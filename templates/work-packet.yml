# Work Packet — contract between agents
# Max 15 bullets of context. No prose.

spec_id: "FEAT-XXX"
phase: "dev"            # spec | design | dev | review | blocked
from_role: "designer"
to_role: "dev"
spec_ref: "./specs/FEAT-XXX.md"

input_refs:
  - path: "./design/FEAT-XXX-decisions.md"
    reason: "design decisions to implement against"

constraints:
  -

acceptance_criteria:
  -

done_when:
  -

project_context:
  stack: ""
  standards_ref: ""     # path to project rules/CLAUDE.md
  other: ""

token_budget: "medium"  # low | medium | high
escalate_if:
  -
